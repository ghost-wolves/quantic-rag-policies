# Copy to .env and fill in values. NEVER commit .env.

# Choose one provider path later (we'll support at least one).
OPENAI_API_KEY=
# Optional: if using another provider later
# ANTHROPIC_API_KEY=
# GOOGLE_API_KEY=

# App settings
FLASK_ENV=development
PORT=5000
